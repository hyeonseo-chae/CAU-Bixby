structure (Datanotice) {
    
   property(totalRecordCount){
    type(totalRecordCount)
    min(Optional) max(Many)
  }
  property(recordCountPerPage){
    type(recordCountPerPage)
    min(Optional) max(Many)
  }
  property(currentPageNo){
    type(currentPageNo)
    min(Optional) max(Many)
  }
  property(jsFunction){
    type(jsFunction)
    min(Optional) max(Many)
  }
  property(list){
    type(Listnotice)
    min(Optional) max(Many)
  }
}